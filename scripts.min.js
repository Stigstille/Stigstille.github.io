document.addEventListener("DOMContentLoaded", function () { let e = new Uint8Array(16); window.crypto.getRandomValues(e); let t = btoa(String.fromCharCode.apply(null, e)); document.querySelector("script").setAttribute("nonce", t); let n = document.querySelector('meta[http-equiv="Content-Security-Policy"]'), o = n.getAttribute("content").replace("nonce-placeholder", "nonce-" + t); n.setAttribute("content", o) }); function sillyGoose() { alert("You should have seen this coming -_-") }